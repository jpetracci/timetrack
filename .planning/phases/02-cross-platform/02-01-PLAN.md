---
phase: 02-cross-platform
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - android/app/src/main/AndroidManifest.xml
  - android/app/build.gradle.kts
  - ios/Runner/Info.plist
  - ios/Podfile
  - web/index.html
  - web/manifest.json
autonomous: true
must_haves:
  truths:
    - "App metadata shows TimeTrack consistently on Android, iOS, and web"
    - "Platform build targets align with Flutter's supported minimum OS versions"
  artifacts:
    - path: android/app/src/main/AndroidManifest.xml
      provides: Android app label metadata
      contains: "android:label=\"TimeTrack\""
    - path: ios/Runner/Info.plist
      provides: iOS display name metadata
      contains: "CFBundleDisplayName"
    - path: web/manifest.json
      provides: Web app name and description
      contains: "\"name\": \"TimeTrack\""
    - path: android/app/build.gradle.kts
      provides: Android minimum SDK target
      contains: "minSdk"
    - path: ios/Podfile
      provides: iOS deployment target
      contains: "platform :ios, '13.0'"
  key_links:
    - from: android/app/build.gradle.kts
      to: "AndroidManifest.xml"
      via: "applicationId and label used by Android build"
      pattern: "applicationId|android:label"
    - from: web/index.html
      to: web/manifest.json
      via: "title + manifest metadata"
      pattern: "<title>|manifest.json"
---

<objective>
Align cross-platform build metadata and minimum OS targets.

Purpose: Ensure iOS, Android, and web builds present consistent app identity and supported targets.
Output: Updated platform config files for display names and minimum OS targets.
</objective>

<execution_context>
@/Users/joel/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/joel/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-cross-platform/02-CONTEXT.md
@.planning/phases/02-cross-platform/02-RESEARCH.md
@android/app/src/main/AndroidManifest.xml
@android/app/build.gradle.kts
@ios/Runner/Info.plist
@ios/Podfile
@web/index.html
@web/manifest.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Align platform display names and metadata</name>
  <files>android/app/src/main/AndroidManifest.xml, ios/Runner/Info.plist, web/index.html, web/manifest.json</files>
  <action>
Set the app display name to "TimeTrack" consistently across Android, iOS, and web:
- Android: update `android:label` in `AndroidManifest.xml`.
- iOS: set `CFBundleDisplayName` and `CFBundleName` to "TimeTrack" in `Info.plist`.
- Web: update `<title>` and web manifest `name`, `short_name`, and `description` to reflect TimeTrack.
Keep icons and other defaults unchanged.
  </action>
  <verify>Confirm the updated strings appear in the platform files listed above.</verify>
  <done>All platform metadata references TimeTrack consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Pin minimum OS targets to supported defaults</name>
  <files>android/app/build.gradle.kts, ios/Podfile</files>
  <action>
Set minimum platform targets to Flutter-supported defaults:
- Android: set `minSdk = 24` in `defaultConfig` (replace `flutter.minSdkVersion`).
- iOS: uncomment and set `platform :ios, '13.0'` in `Podfile`.
Do not modify applicationId or signing configs.
  </action>
  <verify>`flutter build apk --debug` and `flutter build ios --debug --no-codesign` complete without target version errors.</verify>
  <done>Android and iOS build targets are pinned and build successfully.</done>
</task>

</tasks>

<verification>
- Android and iOS builds succeed with the configured minimum targets.
- Web metadata shows TimeTrack for name/title/description.
</verification>

<success_criteria>
- Platform metadata shows TimeTrack on Android, iOS, and web.
- Minimum OS targets set to Android 24+ and iOS 13+ with no build target errors.
</success_criteria>

<output>
After completion, create `.planning/phases/02-cross-platform/02-01-SUMMARY.md`
</output>
